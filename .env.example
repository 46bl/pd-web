# PlayDirty Environment Variables
# 
# DEPLOYMENT ARCHITECTURE:
# - Frontend: IONOS Deploy Now (static hosting) - build from /client
# - Backend: Railway/Render/Replit (Node.js) - run from root
# - Database: Neon/Supabase (PostgreSQL)
#
# SETUP STEPS:
# 1. Create database at neon.tech (free tier)
# 2. Deploy backend to Railway/Render with these env vars
# 3. Deploy frontend to IONOS with VITE_API_URL pointing to backend
# 4. Update CORS_ORIGIN with your IONOS URLs

## Backend (.env in root directory)
# Copy this file to .env and fill in your values

# Database Configuration
# Get from: Neon.tech, Supabase, or Railway PostgreSQL
DATABASE_URL=postgresql://user:password@host:5432/database

# Session Secret (REQUIRED in production - generate a random string)
# Use: openssl rand -base64 32
# IMPORTANT: Never use the default value in production!
SESSION_SECRET=your-session-secret-here-change-this

# CORS Configuration (REQUIRED in production)
# Comma-separated list of allowed frontend origins for CSRF protection
# For IONOS Deploy Now, add your preview and production URLs
# IMPORTANT: Must be exact matches including protocol (https://)
CORS_ORIGIN=https://your-app.ionos.space,https://preview-xyz.ionos.space,https://your-custom-domain.com

# Cookie Domain (optional, only if using custom domain)
# Leave blank for automatic domain detection
COOKIE_DOMAIN=

# Node Environment
NODE_ENV=production

# Port (optional, defaults to 5000)
PORT=5000

## Frontend (client/.env.local or IONOS Deploy Now environment variables)
# For IONOS Deploy Now deployment, add this to your IONOS environment variables

# Backend API URL
# Examples:
#   Railway: https://your-app.up.railway.app
#   Render: https://your-app.onrender.com  
#   Replit: https://your-repl.repl.co
# Leave blank for development (uses same origin)
VITE_API_URL=
